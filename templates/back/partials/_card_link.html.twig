<div class="card" style="width: 18rem;">
  <div class="card-body">
  {# |capitalize pour mettre la majuscule #}
    <h5 class="card-title">{{ entity|capitalize }}</h5>
    <a href="{{ path('app_back_' ~ entity ~ '_new')}}" class="card-link btn btn-success">Create New</a>
    <a href="{{ path('app_back_' ~ entity ~ '_index')}}" class="card-link btn btn-primary">List</a>
  </div>
</div>